plugins {
    id 'java'
    id 'com.gtnewhorizons.retrofuturagradle' version '1.4.9'
}

group = 'ru.arthaix.platelayer'
version = '1.0.0'
archivesBaseName = 'platelayer'

// Minecraft 1.12.2 needs Java 8 bytecode (FML's ASM cannot read newer class files)
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(8)
    }
}

minecraft {
    mcVersion = '1.12.2'
    mcpMappingChannel = 'stable'
    mcpMappingVersion = '39'
    username = 'Dev'
    extraRunJvmArguments.add('-Xmx4G')
}

repositories {
    mavenCentral()
}

dependencies {
    // Immersive Railroading and UniversalModCore are reached by reflection at runtime, never compiled against
}

processResources {
    inputs.property 'version', project.version
    inputs.property 'mcversion', minecraft.mcVersion.get()
    filesMatching('mcmod.info') {
        expand 'version': project.version, 'mcversion': minecraft.mcVersion.get()
    }
}
